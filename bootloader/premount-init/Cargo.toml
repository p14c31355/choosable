[workspace]

[package]
name = "choosable-premount-init"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "premount-init"
path = "src/main.rs"

[dependencies]
libc = "0.2"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true